[project]
name = "illustrator-mcp"
version = "0.1.0"
description = "MCP server for Adobe Illustrator on Windows (COM + ExtendScript)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=2.2,<3",
    "pywin32>=306; sys_platform == 'win32'",
]

[tool.ruff]
line-length = 110

[tool.black]
line-length = 110
